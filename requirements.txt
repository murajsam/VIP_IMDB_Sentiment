# ============================================================================
# Zavisnosti projekta (testirano sa Python 3.14, CPU)
# ----------------------------------------------------------------------------
# PyTorch se instalira sa zvaničnog CPU indeksa. Ako koristite GPU, pratite
# uputstvo sa https://pytorch.org/get-started/locally/
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# ============================================================================

torch>=2.2            # neuronske mreže (instalirati sa CPU indeksa, videti gore)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3     # metrike i StratifiedKFold
scipy>=1.11           # statistički testovi u EDA (point-biserial korelacija)
matplotlib>=3.7
seaborn>=0.12
wordcloud>=1.9        # oblaci reči u EDA
mlflow>=2.9           # praćenje eksperimenata
PyYAML>=6.0           # čitanje configs/experiments.yaml
streamlit>=1.30       # front-end aplikacija (seminarski rad)
